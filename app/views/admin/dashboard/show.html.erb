<header class="page-header">
  <h1> Admin &raquo; Dashboard</h1>
</header>

<div class="panel panel-default">
  <table class="table table-bordered">
    <tbody>
      <tr>
        <td><%= link_to 'Total Products', %i[admin products] %></td>
        <td><%= @total_product_count%></td>
      </tr>
      <tr>
        <td><%= link_to 'Total Categories', %i[admin categories]%></td>
        <td><%= @total_category_count%></td>
      </tr>
      <tr>
        <td><%= link_to 'Total Active Sales', %i[admin sales]%></td>
        <td><%= @total_active_sale_count%></td>
      </tr>
      <tr>
        <td><%= link_to 'Total Upcoming Sales', %i[admin sales]%></td>
        <td><%= @total_upcoming_sale_count%></td>
    </tr>
    </tbody>
  </table>
</div>
